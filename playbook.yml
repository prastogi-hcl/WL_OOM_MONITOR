- name: WebLogic OOM Monitoring & Incident Handling
  hosts: server
  become: yes
  vars_files:
    - vars/main.yml
  tasks:
    - name: Call Check_log dynamically
      include_role:
        name: check_log
